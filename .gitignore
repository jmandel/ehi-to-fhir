# ─────────────────────────────────────────────────────────────────────────────
# PHI / raw source — NEVER commit. This is the patient's full raw record.
# (gitignore does NOT support trailing comments — keep patterns on their own lines)
# ─────────────────────────────────────────────────────────────────────────────
ehi.sqlite
ehi.sqlite-*
raw/
# generated FHIR (lean + bridge) — regenerable + PHI
out/
out-answerkey/
# full per-leaf ledger — regenerable + PHI
compare/LEDGER.json
*.bak
*.bak.json
# old self-contained viewer with an embedded PHI payload (superseded by report/)
viewer.html

# ─────────────────────────────────────────────────────────────────────────────
# Intermediate analysis artifacts — not needed to build the report, and some quote
# real values next to redaction tokens. The distilled, redaction-safe data ships in
# report/viewer/data*.json + report/src/summaries.json instead.
# ─────────────────────────────────────────────────────────────────────────────
report/resources/
report/summaries/
report/slices/
report/CROSS-CUTTING.md
report/SCORECARD-DATA.md

# ─────────────────────────────────────────────────────────────────────────────
# Build artifacts / heavy binaries / caches
# ─────────────────────────────────────────────────────────────────────────────
node_modules/
# built by `bun report/build.ts` (and by CI for Pages)
report/app.js
# local screenshots from the headless smoke test
report/_*.png
# 178 MB HL7 validator — download separately (see README)
tools/validator_cli.jar
bun.lockb
*.log
.DS_Store

compare/RESIDUAL-CLUSTERS.md

# patient→Epic feedback report (contains the patient's clinical PHI; not a public artifact)
feedback/

# temp regression-snapshot dirs (build oracles, not source)
out-golden/
out-answerkey-golden/
compare/CODING-FLOOR-AUDIT.md
compare/TRIAGE.md
compare/DELTAS.md

# locally-built skill zip — CI assembles it from report/health-record-assistant/ (text shell+content)
report/*.zip

# ── submodule-CI rebuild architecture ──
# committed: redacted fhir-target/ + redacted crosswalk/identifiers.csv (answer key for CI)
# local-only unredacted sources:
fhir-target.unredacted/
crosswalk/identifiers.csv.unredacted
# CI-built (derived) — no longer committed; GH Actions rebuilds from the submodule:
report/viewer/data.json
report/viewer/data-lean.json
report/health-record-assistant/data/
out-crosswalk/

# Pages staging dir (CI regenerates from committed inputs)
_site/
